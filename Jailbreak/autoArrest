message = function(callback)
    return require(game:GetService("ReplicatedStorage").Game.Notification).new(callback)
end

message({Text="Auto Arrest is broken right now. We are updating it and should be working in a few hours"})
task.wait(7)
message({Text="Join the discord so you know when we fix it: discord.gg/universalfarm"})
task.wait(7)
message({Text="For now, use the Auto Rob"})
